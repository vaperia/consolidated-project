function createList(items){return items.map(i=>`<li>${i}</li>`).join("");}
function createSkills(skills){return skills.map(s=>`<span class="skill-pill">${s}</span>`).join("");}
function createExternalButton(url,label,primary=false){if(!url||url==="#")return "";return `<a class="btn ${primary?"primary":""}" href="${url}" target="_blank" rel="noopener">${label}</a>`;}
function getProjectId(){return new URLSearchParams(window.location.search).get("project");}
function renderProject(){const id=getProjectId();const p=projects[id];const page=document.getElementById("project-page");if(!p){page.innerHTML=`<section class="project-error"><p class="eyebrow">Portfolio</p><h1>Project not found</h1><p>The requested project could not be found.</p><a class="btn primary" href="index.html">Return to Portfolio</a></section>`;return;}document.title=`Ryan Ang | ${p.title}`;const video=p.video?`<section class="detail-section"><p class="section-kicker">Project Demonstration</p><h2>Video Demo</h2><p class="detail-description">This video demonstrates the project and its implemented system behaviour.</p><div class="detail-video"><iframe src="${p.video}" title="${p.title} demonstration" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe></div></section>`:"";const source=createExternalButton(p.source,"View Source Code ↗",true);const docs=createExternalButton(p.documentation,"View Documentation ↗");page.innerHTML=`<section class="detail-hero"><a href="index.html" class="back-link">← Back to Portfolio</a><p class="eyebrow">${p.category}</p><h1>${p.title}</h1><div class="detail-status"><span>${p.type}</span><span>${p.status}</span></div><p class="detail-intro">${p.overview}</p><div class="skill-strip">${createSkills(p.skills)}</div><div class="hero-actions">${source}${docs}</div></section>${video}<section class="detail-section"><p class="section-kicker">Project Background</p><h2>Project Goal</h2><p class="detail-description">${p.goal}</p></section><section class="detail-section"><p class="section-kicker">Development</p><h2>What Was Done</h2><ul class="detail-list">${createList(p.development)}</ul></section><section class="detail-grid"><article class="detail-card"><p class="section-kicker">Engineering Process</p><h2>Challenges</h2><ul class="detail-list">${createList(p.challenges)}</ul></article><article class="detail-card"><p class="section-kicker">My Work</p><h2>My Contribution</h2><ul class="detail-list">${createList(p.contribution)}</ul></article></section><section class="detail-section learning-section"><p class="section-kicker">Reflection</p><h2>What I Learned</h2><ul class="detail-list">${createList(p.learning)}</ul></section><section class="detail-bottom"><p class="section-kicker">Project Resources</p><h2>Explore the Implementation</h2><p>View the available source code, technical documentation and project materials.</p><div class="hero-actions">${source}${docs}</div></section>`;}
renderProject();
